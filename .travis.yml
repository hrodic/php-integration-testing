language: php

cache:
  directories:
    - $HOME/.composer/cache

env:
  global:
    - PHPUNIT_FLAGS='--testdox --verbose'

sudo: false

notifications:
  email: false

php:
  - 7.3
  - 7.4

matrix:
  fast_finish: true

install:
  - composer update

script: vendor/bin/phpunit $PHPUNIT_FLAGS